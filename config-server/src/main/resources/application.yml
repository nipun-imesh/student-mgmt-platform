server:
  port: 8082

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config
    gcp:
      firestore:
        project-id: ${GCP_PROJECT_ID}
