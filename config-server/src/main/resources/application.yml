server:
  port: 8082

spring:
  application:
    name: course-service
  # Force Servlet (MVC) mode — prevents Firestore's reactive dependency from switching to WebFlux
  main:
    web-application-type: servlet
  cloud:
    gcp:
      firestore:
        project-id: ${GCP_PROJECT_ID}
